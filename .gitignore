node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# Tauri (also ignored by src-tauri/.gitignore; listed here too so tools that only
# read the root .gitignore, like eslint's includeIgnoreFile and prettier, skip them as well)
/src-tauri/target
/src-tauri/gen

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
/test-results
/playwright-report
/blob-report
